# ----- Python -----
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info/
.eggs/
build/
develop-eggs/
downloads/
eggs/
sdist/
var/
wheels/
.installed.cfg

# Virtual environments
.venv/
venv/
env/
ENV/

# ----- Node / Vite (frontend/) -----
node_modules/
dist/

# ----- Runtime / generated data (case-scoped + outputs) -----
# Anchored to repo root — must NOT match core/cases/ which is source code.
/cases/
/output/
# Underscore-prefixed debug captures dropped at the repo root by ad-hoc
# scripts (e.g. _ufs_agentic.json from agentic-extract debugging).
/_*.json

# ----- Repo-specific large or local-only directories -----
/legacy/
/reference/
/input/

# ----- Local secrets / overrides -----
.env
.env.*
!.env.example
config/config.local.yaml
config/secrets.yaml

# ----- IDE / editor -----
.vscode/
.idea/
*.swp
*.swo
*~

# ----- OS noise -----
.DS_Store
Thumbs.db
desktop.ini

# ----- Test artifacts -----
.pytest_cache/
.coverage
htmlcov/
.tox/

# ----- Logs -----
*.log
